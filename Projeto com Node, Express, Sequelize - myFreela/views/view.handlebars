<div id="job-view-container" class="container">
    <div class="row text-center">
        <a class="back-link main-color" href="/">Voltar</a>
        <div id="img-container" class="col-md-12">
            <img class="{{#if job.dataValues.new_job }}new-job{{/if}}" src="/img/company.svg" alt="Company">
        </div>
        <div class="col-md-12">
            <h1 class="main-color {{#if job.dataValues.new_job }}new-job{{/if}}">{{job.dataValues.title}}</h1>
            <p class="bold">Quem eles estão buscando: </p>
            <p class="job-desc">{{job.dataValues.description}}</p>
            <p><span class="bold">O salário informado é de </span><span class="money">${{job.dataValues.salary}}</span></p>
            <p>
                Para trabalhar na <span class="bold main-color {{#if job.dataValues.new_job }}new-job{{/if}}">{{job.dataValues.company}}</span> envie um e-mail para a <a class="main-color {{#if job.dataValues.new_job }}new-job{{/if}}" href="mail:to{{job.dataValues.email}}">{{job.dataValues.email}}</a>
            </p>
        </div>
    </div>
</div>